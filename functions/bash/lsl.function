function lsl (){
    case $# in
        1)  ls -l "$1" |awk '{print $5}'    ;;
        *)  until [[ "$1" = "" ]] ;do
                echo "$(ls -l "$1" |awk '{print $5}') $(basename "$1")"
                shift
            done |sort -rnk1    ;;
    esac
    return 0
}

